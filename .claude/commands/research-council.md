---
description: Convene a council of parallel research agents on a topic — multi-perspective synthesis with citations
argument-hint: <topic>
---

Invoke the `research-council` skill to run a full council session on: $ARGUMENTS

Follow the skill's workflow exactly: parse → pick lenses → confirm composition → dispatch agents in **parallel** (single message, multiple Agent tool calls) → synthesize → deliver report.
